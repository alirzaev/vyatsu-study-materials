<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.rzaevali.resistorcolor">

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/CustomTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/mainActivityTitle"
            android:screenOrientation="sensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ResistorsActivity"
            android:label="@string/resistorsActivityTitle"
            android:screenOrientation="sensor" />
        <activity
            android:name=".ColorChooserActivity"
            android:label="@string/colorChooserActivityTitle"
            android:screenOrientation="sensor" />
        <activity
            android:name=".TextRActivity"
            android:label="@string/textRActivityTitle"
            android:screenOrientation="sensor" />
    </application>

</manifest>